server:
  port: 8082

spring:
  application:
    name: beneficiary-service
  datasource:
    url: jdbc:mysql://localhost:3306/beneficiary_service_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

info:
  app:
    name: Beneficiary Service
    description: Manages beneficiary CRUD and validation for fund transfers
