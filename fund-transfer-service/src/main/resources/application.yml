spring:
  application:
    name: fund-transfer-service
  config:
    import: optional:configserver:http://localhost:8888

# Port (8083), datasource, JPA, Eureka, Feign timeouts, Swagger, Actuator
# settings are all centrally defined in
# config-server/src/main/resources/config-repo/fund-transfer-service.yml
# and config-repo/application.yml (shared defaults)
